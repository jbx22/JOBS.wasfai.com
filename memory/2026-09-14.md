## jobs58 — 2026-09-14 00:25

- Add a first-class public release marker: `functions/api/version.js` publishes the `JOBS_RELEASE` constant at `GET /api/version`, with an `x-jobs-release` response header and no-store caching. The service worker `CACHE_NAME` is no longer the only deployed release identity. Unauthenticated and secret-free; pinned by `test/release-marker.test.mjs` against `VERSION.json`.
- Note: `jobs56`/`jobs57` were used as labels in commit subjects on 2026-09-13 but never recorded in `VERSION.json`/`VERSIONS.md`, so this bump records the next free number (58).
